import { PreserveNewLinesPipe } from './preserve-new-lines.pipe';

describe('PreserveNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new PreserveNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
